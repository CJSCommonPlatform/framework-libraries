<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>uk.gov.justice</groupId>
        <artifactId>maven-framework-parent-pom</artifactId>
        <version>25.104.0-M3</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <groupId>uk.gov.justice.framework.libraries</groupId>
    <artifactId>framework-libraries</artifactId>
    <version>25.104.0-M7-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Framework Common Libraries</name>
    <description>Common Libraries used by Microservice Framework</description>

    <modules>
        <module>annotation-validator</module>
        <module>domain-test-dsl</module>
        <module>framework-api</module>
        <module>framework-libraries-bom</module>
        <module>framework-utilities</module>
        <module>generator-maven-plugin</module>
        <module>job-manager</module>
        <module>json-schema-catalog</module>
        <module>json-transformer</module>
        <module>jsonschema-pojo-generator</module>
        <module>raml-maven</module>
        <module>framework-datasources</module>
        <module>framework-libraries-version</module>
    </modules>

    <properties>
        <cpp.repo.name>framework-libraries</cpp.repo.name>
        <maven-common-bom.version>25.104.0-M2</maven-common-bom.version>
        <cucumber.reporting.plugin.version>2.0.0</cucumber.reporting.plugin.version>
        <framework-wiremock-service.version>25.104.0-M3</framework-wiremock-service.version>
        <file-service.version>25.104.0-M3</file-service.version>

        <!--
            jakarta.xml.bind:jakarta.xml.bind-api is managed by the BOM at 4.0.0 (Jakarta EE 10,
            jakarta.* namespace). However, org.raml:raml-parser is a legacy third-party library
            that uses javax.xml.bind.* classes internally. Those classes only exist in versions
            2.x of jakarta.xml.bind-api (before the EE 9 namespace change). From 3.0.0 onwards
            the namespace became jakarta.xml.bind.*  and javax.xml.bind.* is no longer provided.
            Any module that runs the RAML parser at compile or test time must pin this dependency
            to ${jakarta.xml.bind-api.raml.version} to avoid NoClassDefFoundError on
            javax/xml/bind/SchemaOutputResolver. DO NOT upgrade to 3.x or 4.x.
        -->
        <jakarta.xml.bind-api.raml.version>2.3.2</jakarta.xml.bind-api.raml.version>

    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>uk.gov.justice</groupId>
                <artifactId>maven-common-bom</artifactId>
                <version>${maven-common-bom.version}</version>
                <scope>import</scope>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.framework-api</groupId>
                <artifactId>framework-api-unifiedsearch</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.framework-api</groupId>
                <artifactId>framework-api-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.utils</groupId>
                <artifactId>utilities-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.utils</groupId>
                <artifactId>utilities-file</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.utils</groupId>
                <artifactId>test-utils-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>uk.gov.justice.maven</groupId>
                <artifactId>maven-test-utils</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <scm>
        <connection>${cpp.scm.connection}</connection>
        <developerConnection>${cpp.scm.developerConnection}</developerConnection>
        <url>${cpp.scm.url}</url>
    </scm>

    <build>

        <plugins>
            <plugin>
                <groupId>org.eluder.coveralls</groupId>
                <artifactId>coveralls-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
                <dependencies>
                    <!-- Pinned to ${jakarta.xml.bind-api.raml.version} (pre-EE9): org.raml:raml-parser uses javax.xml.bind.* which is only present in jakarta.xml.bind-api 2.x. See root pom for full explanation. -->
                    <dependency>
                        <groupId>jakarta.xml.bind</groupId>
                        <artifactId>jakarta.xml.bind-api</artifactId>
                        <version>${jakarta.xml.bind-api.raml.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>

    </build>

    <profiles>
        <profile>
            <id>integration-tests</id>
            <activation>
                <property>
                    <name>run.it</name>
                    <value>!false</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <configuration>
                            <systemPropertyVariables>
                                <random.jms.port>${random-jms-port}</random.jms.port>
                                <random.http.port>${random-http-port}</random.http.port>
                            </systemPropertyVariables>
                        </configuration>
                    </plugin>

                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>reserve-jms-port</id>
                                <goals>
                                    <goal>reserve-network-port</goal>
                                </goals>
                                <phase>process-resources</phase>
                                <configuration>
                                    <portNames>
                                        <portName>random-jms-port</portName>
                                        <portName>random-http-port</portName>
                                        <portName>random-management-port</portName>
                                    </portNames>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
